import React from 'react';

import Layout from '@common/Layout';
import { Container } from '@components/global';

const NotFoundPage = () => (
  <Layout>
    <Container>
      <h1>YANLIŞ SAYFA</h1>
      <p>Geldiğiniz sayfaya geri dönün.</p>
    </Container>
  </Layout>
);

export default NotFoundPage;
